import { customButtonDirective } from './c-btn.directive';

describe('customButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new customButtonDirective();
    expect(directive).toBeTruthy();
  });
});
